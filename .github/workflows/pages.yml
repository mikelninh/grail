name: pages

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "dashboard/**"
      - "api/**"
      - "src/grail/**"
      - "data/watchlist.json"
      - "data/token_mappings.json"
      - "data/provider_owner_targets.json"
      - "scripts/**"
      - ".github/workflows/pages.yml"
  schedule:
    - cron: "37 * * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e .
      - run: python -m unittest discover -s tests -v
      - name: Build live market, sales, inventory and evidence graph
        run: python scripts/build_pages.py
      - name: Enrich provider-chain ownership golden cases
        run: python scripts/enrich_provider_ownership.py
      - name: Resolve exact Collect Chain owners and transfers from verified registry
        run: python scripts/enrich_ownership.py
      - name: Update forward-test alpha ledger
        run: python scripts/build_forward.py
      - name: Persist evidence state
        run: |
          mv data/forward_state.next.json data/forward_state.json
          mv data/inventory_state.next.json data/inventory_state.json
          git config user.name "grail-evidence-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/forward_state.json data/inventory_state.json
          if ! git diff --cached --quiet; then
            git commit -m "data: update GRAIL evidence state [skip ci]"
            git push
          fi
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  public-health:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Verify public HTML and data endpoints
        env:
          PAGE_URL: ${{ needs.deploy.outputs.page_url }}
        run: |
          set -euo pipefail
          echo "Checking ${PAGE_URL}"
          for attempt in 1 2 3 4 5 6; do
            html=$(curl -fsSL --max-time 20 "${PAGE_URL}?health=${GITHUB_RUN_ID}" || true)
            json=$(curl -fsSL --max-time 20 "${PAGE_URL}data/opportunities.json?health=${GITHUB_RUN_ID}" || true)
            if [[ "$html" == *"GRAIL"* && "$json" == *'"candidates"'* ]]; then
              echo "PUBLIC HEALTH PASS on attempt ${attempt}"
              exit 0
            fi
            echo "Attempt ${attempt} not healthy yet; retrying"
            sleep 10
          done
          echo "PUBLIC HEALTH FAIL: Pages deployment reported success but public endpoints are not serving expected content"
          exit 1
